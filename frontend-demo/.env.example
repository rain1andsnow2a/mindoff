# 复制成 .env 即生效（Expo 会把 EXPO_PUBLIC_* 注入打包产物）。
# 不设置时用 src/api.ts 里的 DEFAULT_API_BASE = http://223.109.142.152:8000（线上 Docker 实例）。
#
# 本地联调后端：
#   浏览器预览        EXPO_PUBLIC_API_BASE=http://127.0.0.1:8000
#   Android 模拟器    EXPO_PUBLIC_API_BASE=http://10.0.2.2:8000
#   Android 真机      EXPO_PUBLIC_API_BASE=http://<这台电脑的WLAN IPv4>:8000
# 改完要重启 Expo（APK 需重新构建），环境变量是编译期注入的。
EXPO_PUBLIC_API_BASE=http://223.109.142.152:8000
